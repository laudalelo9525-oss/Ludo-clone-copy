# scripts/

Developer and operations helper scripts.

| Script          | Purpose                                                          |
| --------------- | ---------------------------------------------------------------- |
| `dev-stack.sh`  | Start PostgreSQL + Redis in Docker and run the backend in watch mode |

Run from the repository root, e.g. `scripts/dev-stack.sh --deps`.
